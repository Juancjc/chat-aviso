services:
  app:
    image: sinaliza-chat-aviso:local
    environment:
      REVERB_APP_KEY: ${REVERB_APP_KEY:-sinaliza-chat-aviso-key}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_REVERB_APP_KEY: ${REVERB_APP_KEY:-sinaliza-chat-aviso-key}
